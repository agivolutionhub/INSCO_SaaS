# Configuración de OpenAI
# IMPORTANTE: Reemplaza los valores de ejemplo con tus propias claves API
# Puedes obtener tu API key en: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_ASSISTANT_ID=asst_xxxxxxxxxxxxxxxxxxxxxxxx

# Otras configuraciones (opcional)
DEBUG=false
LOG_LEVEL=info

# NOTA: Copia este archivo a las siguientes ubicaciones:
# 1. ./backend/config/.env (principal)
# 2. ./.env (respaldo)
#
# INSTRUCCIONES DE DESPLIEGUE:
# - Asegúrate de que el archivo .env contenga tus claves API reales
# - Verifica que docker-compose.yml incluya el volumen: ./backend/config/.env:/app/.env
# - Ejecuta: docker-compose up --build -d 